// Removes the last item from an array and return it
const numbers = [1, 2, 3, 4]

console.log('Initial array before pop', numbers)

const lastItem = numbers.pop()

console.log('Array after pop', numbers)
console.log('Popped item', lastItem)