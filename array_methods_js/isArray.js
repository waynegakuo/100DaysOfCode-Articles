// Checks if the passed value is an array

const names = ['Florin', 'Ivan', 'Liam']
const str = 'Hello World'
const number = 17

//isArray method leaves in the Array class so it should always follow 'Array.'
console.log(Array.isArray(names))