// Allows you to add one or more elements at the end of the array and returns the new length of the array
const numbers = [1, 2, 3]
const total = numbers.push(4, 5, 6, 7);

console.log(numbers)
console.log('The length of this array is: ', total)